providers:
  - name: default
    type: file
    updateIntervalSeconds: 10   # picks up dashboard.json changes without restart
    disableDeletion: false
    allowUiUpdates: true        # lets you edit panels live during demo
    options:
      path: /var/lib/grafana/dashboards